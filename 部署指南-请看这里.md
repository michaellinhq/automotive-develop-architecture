# 🚀 部署指南 - 3步完成

## 📍 第1步：找到项目文件夹

你的项目文件夹就是你之前在 Cowork 中选择的那个文件夹。

**快速找到方法：**
- 在 Finder 中，查看你之前选择的文件夹
- 文件夹名称可能是：`automotive-develop-architecture`
- 通常位置在：`~/Desktop/` 或 `~/Documents/` 或 `~/Developer/`

---

## 📍 第2步：打开终端

**方法A：** 从 Finder 打开
1. 在 Finder 中找到项目文件夹
2. 右键点击文件夹
3. 选择"服务" → "新建位于文件夹位置的终端窗口"

**方法B：** 手动导航
```bash
# 打开终端应用
# 然后 cd 到你的项目文件夹，例如：
cd ~/Desktop/automotive-develop-architecture
# 或
cd ~/Documents/automotive-develop-architecture
# 或
cd ~/Developer/automotive-develop-architecture
```

---

## 📍 第3步：运行部署命令

在终端中依次执行以下命令：

### 3.1 清理锁文件（如果有）
```bash
rm -f .git/index.lock
```

### 3.2 查看待提交的文件
```bash
git status
```

你应该看到以下文件：
- ✅ mkdocs.yml
- ✅ overrides/main.html
- ✅ stylesheets/extra.css
- ✅ docs/javascripts/language-switcher.js
- ✅ 以及几个文档文件

### 3.3 创建提交
```bash
git commit -m "Add bilingual language switcher

- Added floating language switcher
- Material Design UI
- Responsive design
- Dark mode support

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>"
```

### 3.4 推送到GitHub
```bash
git push origin main
```

### 3.5 部署到网站
```bash
mkdocs gh-deploy --force
```

---

## ✅ 完成！

部署成功后，等待 **3-5分钟**，然后访问：
### 🌐 https://docs.compliance-waechter.com

你会看到右上角出现一个蓝色的语言切换按钮！

---

## ❓ 遇到问题？

### Q: 找不到项目文件夹？
在终端运行：
```bash
find ~ -name "automotive-develop-architecture" -type d 2>/dev/null
```

### Q: git push 需要密码？
使用 GitHub Personal Access Token：
1. 访问：https://github.com/settings/tokens
2. 生成新 token（repo 权限）
3. 使用 token 作为密码

### Q: mkdocs 命令找不到？
安装 MkDocs：
```bash
pip3 install mkdocs-material mkdocs-static-i18n
```

---

## 🎯 一键脚本（可选）

如果你在正确的项目文件夹中，也可以直接运行：

```bash
./force-deploy.sh
```

这个脚本会自动完成所有步骤！

---

<div align="center">

**💪 你可以的！就3个命令！**

1️⃣ `rm -f .git/index.lock`
2️⃣ `git commit -m "Add language switcher"`
3️⃣ `git push && mkdocs gh-deploy --force`

</div>

---

## 📧 需要帮助？

如果还是不行，发邮件给我：
**haiqing.lin@compliance-waechter.com**

附上终端的错误信息截图。
